<%= form_for(@subscribtion) do |f| %>
  <% if @subscribtion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subscribtion.errors.count, "error") %> prohibited this subscribtion from being saved:</h2>

      <ul>
      <% @subscribtion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
