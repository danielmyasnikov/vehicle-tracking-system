<%= form_for(target,
      remote: true, format: :json,
      html: { class: "edit-form form-inline" }) do |f| %>
  <div class="field">
    <%= f.check_box attribute, class: "edit-input #{css_class}" %>
    <%= spinner_tag %>
  </div>
<% end %>
