<%= render :partial => "shared/informative_table", :locals => {
	:items => @fleets,
	:table_name => current_user.truck_fleet.name,
	:columns_to_sort => @columns_to_sort,
	:new_item_path => new_fleet_path,
	:new_action_required => true,
	:columns_to_display => [:fleet_number, :registration, :vin, :make, :model, :year, :auto_services],
	:model_class => Fleet
} %>
