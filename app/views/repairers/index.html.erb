<%= render :partial => "shared/informative_table", :locals => {
	:items => @repairers,
	:table_name => "MyRepairers table",
	:columns_to_sort => ['1', '2'],
	:new_item_path => new_repairer_path,
	:new_action_required => true,
	:columns_to_display => [:business_name, :picture, :full_address, :phone_number, :suburb, :state],
	:model_class => FaultBook
} %>