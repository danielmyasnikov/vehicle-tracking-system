<%= render :partial => "shared/informative_table", :locals => {
	:items => @drivers,
	:table_name => "My Team",
	:columns_to_sort => ['1', '2'],
	:new_item_path => new_driver_path,
	:new_action_required => true,
	:columns_to_display => [:name, :phone_no, :dl_number, :dl_expiry],
	:model_class => Driver
} %>